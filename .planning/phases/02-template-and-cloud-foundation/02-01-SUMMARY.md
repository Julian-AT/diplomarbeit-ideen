# Summary 02-01: Template baseline imported

Imported the Vercel Chatbot template at commit `2becdb4a56e7683ae08aef927cec1c6c52dfad5e` and retained the template app structure. The repo now has the Next.js app, Auth.js flow, database migrations, artifact handlers, editor/code support, Playwright tests, and deployment metadata needed for downstream work.

Changed files include the new app/template tree, `package.json`, `pnpm-lock.yaml`, `README.md`, and merged `.gitignore` rules.
