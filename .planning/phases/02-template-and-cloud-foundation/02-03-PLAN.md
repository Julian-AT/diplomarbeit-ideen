# Plan 02-03: Establish Vitest and Playwright command baseline

## Scope

Create lean test commands for Phase 2 and ensure the imported Playwright harness is callable without Unix-only shell syntax.

## Work Completed

- Added `vitest` as a dev dependency.
- Added `cross-env` as a dev dependency.
- Added `pnpm test:unit` as `vitest run tests/unit`.
- Replaced the default `test` script with `pnpm test:e2e`.
- Added `pnpm test:e2e` using `cross-env PLAYWRIGHT=True pnpm exec playwright test`.
- Added `pnpm typecheck`.
- Added `tests/unit/env.test.ts` for env contract coverage.
- Fixed imported Biome config by removing stale `nursery.noUnnecessaryConditions` and excluding `.planning/**` from source formatting.

## Acceptance Notes

The focused unit suite passes. The Playwright command path is valid and lists imported tests. Full E2E execution is deferred until `.env.local` contains real service credentials and the dev server can talk to its backing services.
