# Plan 02-01: Clone/adapt Vercel Chatbot template and preserve baseline features

## Scope

Import the official Vercel Chatbot template into the project and keep its baseline product surface intact.

## Work Completed

- Cloned `vercel/ai-chatbot` to `C:\tmp\ai-chatbot-template` with depth 1.
- Pinned imported template commit: `2becdb4a56e7683ae08aef927cec1c6c52dfad5e`.
- Copied template app files into the repo, excluding `.git` and preserving the project `.gitignore` for a controlled merge.
- Kept template directories for app routes, chat components, artifacts, DB schema/migrations, tests, public assets, hooks, editor support, and deployment config.
- Updated README to identify the project and template provenance.

## Acceptance Notes

The imported template still contains the baseline chat route, Auth.js pages/actions, persistence layer, artifacts system, multimodal-capable input components, and code artifact infrastructure. Product-specific retrieval tools and `thesis-proposal` artifact are intentionally deferred to later phases.
