# Plan 02-02: Add cloud environment validation and local setup documentation

## Scope

Define and validate the cloud env contract before runtime calls to Vercel services, Qdrant Cloud, Neon/Postgres, Redis, Vercel Blob, AI Gateway, and Gemini embeddings.

## Work Completed

- Added project env keys to `.env.example`:
  - `AUTH_SECRET`
  - `AI_GATEWAY_API_KEY`
  - `BLOB_READ_WRITE_TOKEN`
  - `POSTGRES_URL`
  - `REDIS_URL`
  - `QDRANT_URL`
  - `QDRANT_API_KEY`
  - `QDRANT_COLLECTION`
  - `GEMINI_API_KEY`
  - `GEMINI_EMBEDDING_MODEL`
- Added `lib/env/project.ts` with a zod-backed validation contract and placeholder-aware example validation.
- Added `scripts/validate-env.ts` CLI.
- Added `pnpm env:check` and `pnpm env:check:example` scripts.
- Added `docs/local-setup.md` with local setup, required services, and verification commands.

## Acceptance Notes

`pnpm env:check:example` validates the committed example and accepts placeholder secrets as warnings. `pnpm env:check` validates `.env.local` strictly and is expected to fail until real cloud credentials are provided.
