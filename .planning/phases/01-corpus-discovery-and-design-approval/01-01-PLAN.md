---
phase: 1
plan: 01-01
type: implementation
wave: 1
depends_on: []
files_modified:
  - scripts/corpus_discovery.py
  - .planning/corpus/archive-inspection.json
  - .planning/corpus/manifest.json
  - .planning/corpus/inventory-summary.json
autonomous: true
requirements:
  - CORP-01
  - CORP-02
---

# Plan 01-01: Locate Archive, Unpack Safely, and Inventory Files

<objective>
Locate the single thesis archive, inspect it for unsafe paths, unpack it into `.planning/corpus/raw/`, and produce a machine-readable file inventory.
</objective>

<tasks>
<task id="01-01-T1" type="implementation">
<read_first>
- `.planning/phases/01-corpus-discovery-and-design-approval/01-CONTEXT.md`
- `.planning/ROADMAP.md`
- `.planning/REQUIREMENTS.md`
</read_first>
<action>
Create and run `scripts/corpus_discovery.py` with bounded archive discovery over `.`, `data/`, `data/raw/`, `archive/`, `archives/`, `corpus/`, `theses/`, and `input/`. Accept only one candidate archive unless an explicit path is provided. Refuse unsafe ZIP entries containing absolute paths, `..`, Windows drive separators, or backslashes.
</action>
<acceptance_criteria>
- `scripts/corpus_discovery.py` exists.
- `.planning/corpus/archive-inspection.json` records `_SYPCopilot-Share (1).zip`.
- `.planning/corpus/archive-inspection.json` has `unsafe_entries: []`.
- `.planning/corpus/raw/.extract-complete.json` exists after extraction.
</acceptance_criteria>
</task>

<task id="01-01-T2" type="implementation">
<read_first>
- `scripts/corpus_discovery.py`
- `.planning/corpus/archive-inspection.json`
</read_first>
<action>
Build `manifest.json` and `inventory-summary.json` with per-file relative path, suffix, size, project slug, and document type. Derive project slugs from the `syp-projekte/{project}` folder convention when present.
</action>
<acceptance_criteria>
- `.planning/corpus/manifest.json` contains 86 file records.
- `.planning/corpus/inventory-summary.json` lists extension counts for `.pdf`, `.mp4`, `.pptx`, `.png`, `.mov`, and `.docx`.
- `.planning/corpus/inventory-summary.json` includes per-project folder summaries.
</acceptance_criteria>
</task>
</tasks>

<verification>
Run `python scripts/corpus_discovery.py --archive "_SYPCopilot-Share (1).zip"` and verify it exits 0.
</verification>

<success_criteria>
- The corpus archive is located and unpacked safely.
- The user can inspect file inventory and project folder evidence.
</success_criteria>

## Artifacts this phase produces

- `scripts/corpus_discovery.py`
- `.planning/corpus/archive-inspection.json`
- `.planning/corpus/manifest.json`
- `.planning/corpus/inventory-summary.json`

<must_haves>
truths:
  - statement: "Archive discovery refuses unsafe ZIP paths before extraction."
    verification: "archive-inspection.json has unsafe_entries: [] and extraction marker exists."
  - statement: "Inventory captures all 86 archive files with project slugs and document types."
    verification: "manifest.json file count equals archive file count."
</must_haves>
