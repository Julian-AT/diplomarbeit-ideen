# Plan 03-03: Implement retrieval wrapper and focused unit tests

## Scope

Expose a reusable hybrid query wrapper and tests for chunking, sparse vector behavior, and Qdrant request shapes.

## Work Completed

- Added `queryHybridPriorWork` for dense+sparse prefetch with RRF fusion.
- Added `buildPayloadFilter` for approved filter fields.
- Added unit tests:
  - `tests/unit/retrieval/chunking.test.ts`
  - `tests/unit/retrieval/sparse.test.ts`
  - `tests/unit/retrieval/qdrant.test.ts`
- Existing env validation unit tests remain in place.

## Acceptance Notes

Unit tests verify deterministic chunk IDs, multilingual sparse tokenization, stable sparse vectors, Qdrant named-vector point shape, collection/index creation requests, and filtered RRF query construction.
