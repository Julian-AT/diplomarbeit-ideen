# Summary 03-02: Hybrid indexing primitives implemented

Added sparse encoding, Gemini embedding adapter, Qdrant collection/index/upsert wrapper, and ingestion CLI. The dry-run path can build the full chunk/sparse-vector corpus without secrets. Real upsert is available through `pnpm corpus:ingest` once the env contract is complete.
