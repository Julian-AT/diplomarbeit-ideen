# Plan 04-02: Implement thesis-proposal artifact and structured citation persistence

## Scope

Add a custom proposal artifact kind that can stream cited Markdown, be edited, and participate in the template's document versioning flow.

## Work Completed

- Added `artifacts/thesis-proposal/server.ts` with a citation-preserving proposal prompt and update path.
- Added `artifacts/thesis-proposal/client.tsx` using the existing editor, diff view, document skeleton, copy/version actions, and proposal-specific toolbar actions.
- Registered the artifact in `components/chat/artifact.tsx`, `lib/artifacts/server.ts`, `lib/db/schema.ts`, and `lib/ai/tools/create-document.ts`.
- Added `thesis-proposal` to the document kind enum used by persistence and artifact creation.

## Acceptance Notes

The artifact reuses the template's persistence and versioning surface. Runtime creation still depends on live chat/model credentials.