# Plan 04-01: Implement AI SDK tools and grounding prompt for both chat modes

## Scope

Wire retrieval-backed prior-work tools into the chat route and update prompts so both Spark-to-ideas and Build-on-top flows are grounded before ideas are proposed.

## Work Completed

- Added `lib/retrieval/local.ts` for local JSONL chunk search and thesis-context fallback.
- Added `lib/retrieval/service.ts` to select Qdrant/Gemini retrieval when env is complete and local retrieval otherwise.
- Added `lib/ai/tools/prior-work.ts` with `searchPriorWork`, `getThesisById`, and `findThesisExtensions`.
- Registered the tools in `app/(chat)/api/chat/route.ts` and `lib/types.ts`.
- Updated `lib/ai/prompts.ts` so thesis-topic requests must use prior-work tools, cite source paths, and surface weak evidence.

## Acceptance Notes

The tool layer can be exercised locally through dry-run chunks and is ready for Qdrant-backed runtime retrieval once full credentials are provided.